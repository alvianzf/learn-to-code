// lesson 7

 // checking data types

 /**
  * Sometimes, we need to check the data type of a variable we are working on
  * so that we could know what methods can apply to it
  * 
  * to check the type, we can utilize a function called typeof()
  */

  var num = 6;
  var str = 'string'

  console.log(typeof(num)) // will return number
  console.log(typeof(str)) // will return string 